<div class="back">
  <%= render "layouts/header" %>
  <div class='container'>
    <div class='row'>

      <div class='col-md-3'>
        <div class="top-text box15">
          <table class='table'>
            <tr><h2>ジャンル検索</h2></tr>
            <% @genres.each do |a| %>
              <tr><td><%= link_to a.name , items_path(genre_id: a.id), class: "link_new"%></td></tr>
            <% end %>
          </table>
        </div>
      </div>
      <div class='col-xs-9 box15'>
        <div class="text-center ">
          <h1 ><%= @genre.name %>一覧</h1>
        </div>

            <ul class="bxslider">
                <% @items.each do |item| %>
                  <li>
                    <% if item.genre.status == "有効" && item.status == "販売中" %>
                      <div class ='item animation '>
                        <%= link_to item_path(item.id), class: "link_new" do %>
                          <%= attachment_image_tag item, :item_image,format: 'jpeg', fallback: "no_image.jpg", size: "200x200" %>
                        <% end %>
                    <% end %>
                        <br>
                        <div class="text-center">
                          <%= link_to item.name, item_path(item.id), class: "link_new" %><%= item.name %>
                          <br>
                          <%= item.name %>
                          <font color='rgb( 244, 244, 244);'>¥<%= item.excluded %></font>
                        </div>
                      </div>
                  </li>
                <% end %>
            </ul>

           
        
      </div>

    </div>
  </div>
  <%= paginate(@items) %>
</div>
<footer >
          <div id="footer">
            <p class="footer-text">店舗情報</p>
            <p class="footer-text">Aglicake</p>
            <p class="footer-text">〒000-00000 東京都 TEL：000-0000-0000</p>
            <p class="footer-text">平日 11:00～19:00／土日祝日 9:00～19:00 月曜定休（月曜祝日の場合は翌日休み)</p>
            <p class="footer-text">こちらのサイトからのご注文は24時間受け付けております</p>
           </div>
      </footer>