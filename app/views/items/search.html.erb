<div class="back" >
<%= render "layouts/header" %>
<div class='container'>



<div class='col-md-6 col-md-offset-3'>

      <table>
        <tr>
          <th></th>

          <th>商品名</th>
          <th>税抜価格</th>
        </tr>

        <% @items.each do |item| %>
          <% if item.genre.status == "有効" && item.status == "販売中" %>

          <tr>
            <td><%= attachment_image_tag item, :item_image, format: 'jpeg', fallback: "no_image.jpg", size: "150x150" %></td>
            <td><%= link_to item.name, item_path(item.id) %></td>
            <td><%= item.excluded %></td>
          </tr>
        <% end %>
        <% end %>


</table>
</div>
</div>

</div>
<footer >
          <div id="footer">
            <p class="footer-text">店舗情報</p>
            <p class="footer-text">Aglicake</p>
            <p class="footer-text">〒000-00000 東京都 TEL：000-0000-0000</p>
            <p class="footer-text">平日 11:00～19:00／土日祝日 9:00～19:00 月曜定休（月曜祝日の場合は翌日休み)</p>
            <p class="footer-text">こちらのサイトからのご注文は24時間受け付けております</p>
           </div>
      </footer>